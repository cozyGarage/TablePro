# cargo-audit config (project-local: linux/.cargo/audit.toml)
# Mirrors deny.toml advisory policy for CI.

[advisories]
ignore = [
    # No fixed release. Pulled by russh (SSH) and sqlx-mysql (auth plugins).
    "RUSTSEC-2023-0071",
    "RUSTSEC-2026-0235",
]
